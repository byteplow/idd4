{{template "ui_top_wrapper" . }}
<div class="card">
    {{template "ui_header" "Invite"}}
    <div class="mb-10"><code class="swatch-white">{{.invite}}</code></div>
    <a class="swatch-primary button outline" href="{{.urls.welcome_url}}">back</a>
</div>
{{template "ui_bottom_wrapper" . }}